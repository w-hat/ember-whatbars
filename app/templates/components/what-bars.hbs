{{#each pieces as |piece|}}
  {{#if piece.component}}
    {{component piece.component positional=piece.positional params=piece.params block=piece.block}}
  {{else}}
    {{what-html piece.string sanitizer}}
  {{/if}}
{{else}}
  <!-- whatbars default -->
{{/each}}
