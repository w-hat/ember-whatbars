{{#each pieces as |piece|}}
  {{#if piece.component}}
    {{component piece.component params=piece.params positional=piece.positional}}
  {{else}}
    {{what-html piece.string sanitizer}}
  {{/if}}
{{else}}
  <!-- whatbars default -->
{{/each}}
